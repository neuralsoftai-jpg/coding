This directory is for locally hosted external libraries to avoid network loading errors.

- Downloaded Google Fonts Orbitron and Poppins CSS and font files.
- Downloaded Font Awesome CSS and webfonts.
- Downloaded particles.js library.

Update index.html to reference these local files instead of CDN URLs.
